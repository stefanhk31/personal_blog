name: template_engine
description: Package to build stringified HTML with dynamic data from templates.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  blog_models:
    path: ../blog_models
  intl: ^0.20.2
  logging: ^1.3.0

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.0.0
